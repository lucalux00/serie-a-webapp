"use client";import {useState}from'react';export default function PromoCodeRedeemer(){const[code,setCode]=useState('');const[msg,setMsg]=useState('');async function redeem(){const r=await fetch('/api/promotions/redeem',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});const d=await r.json();setMsg(d.message||d.error||'Errore');if(r.ok)setCode('');}return <div className="rounded-xl border border-[#F59E0B]/30 bg-[#1E293B] p-4"><b className="text-white">Codice Pro o sconto</b><p className="mt-1 text-xs text-[#94A3B8]">Inserisci un codice regalo o promozionale.</p><div className="mt-3 flex gap-2"><input value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="ES. PRO2026" className="min-w-0 flex-1 rounded-lg bg-[#0F172A] p-2 text-sm text-white"/><button onClick={redeem} className="rounded-lg bg-[#F59E0B] px-3 text-xs font-black text-[#0F172A]">APPLICA</button></div>{msg&&<p className="mt-2 text-xs text-[#FCD34D]">{msg}</p>}</div>}
