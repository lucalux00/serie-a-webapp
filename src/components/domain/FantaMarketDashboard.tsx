"use client";
import { useState } from 'react';
import useSWR from 'swr';
import { Check, Plus, Target } from 'lucide-react';
import PremiumPaywall from '@/components/ui/PremiumPaywall';
import { useSubscription } from '@/contexts/SubscriptionContext';

const fetcher = async (url: string) => { const response = await fetch(url); return { status: response.status, body: await response.json() }; };
type Candidate = { name: string; team: string; marketValue: string; costBand: string; score: number; priority: number; reason: string; stats: { seasons: number; appearances: number; minutes: number; goals: number; assists: number; rating: number; goalActions90: number; formDelta: number } };

export default function FantaMarketDashboard({ onOpenRoster: _ }: { onOpenRoster: () => void }) {
  const [role, setRole] = useState<string | null>(null); const [adding, setAdding] = useState<string | null>(null); const [added, setAdded] = useState<string[]>([]);
  const { isPremium, isLoading: subscriptionLoading } = useSubscription();
  const { data, mutate } = useSWR(isPremium ? '/api/fantacalcio/market-ai' : null, fetcher); const { data: recommendations, mutate: refresh } = useSWR(isPremium && role ? `/api/fantacalcio/market-ai?role=${role}` : null, fetcher);
  if (subscriptionLoading) return <p className="p-6 text-slate-400">Verifico il tuo accesso Fanta Pro…</p>;
  if (!isPremium) return <section className="space-y-4"><div className="rounded-3xl border border-amber-400/30 bg-gradient-to-br from-[#35200a] via-[#1E293B] to-[#162033] p-6"><Target className="mb-2 text-amber-400"/><p className="text-xs font-black uppercase tracking-widest text-amber-300">Mercato AI · Fanta Pro</p><h2 className="mt-1 text-2xl font-black text-white">Rinforzi calcolati sulla tua rosa</h2><p className="mt-3 text-sm leading-relaxed text-slate-300">Scopri i reparti scoperti, i sostituti verificati e la priorità d’acquisto: i risultati si attivano con Fanta Pro.</p></div><PremiumPaywall planName="Fanta Pro" price="€0,99" priceLabel="/ mese" ctaLabel="Scopri l’accesso Pro" /></section>;
  async function add(candidate: Candidate) { if (!role) return; setAdding(candidate.name); const response = await fetch('/api/fanta-roster', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ action: 'add', player_name: candidate.name, team_name: candidate.team, role }) }); if (response.ok) { setAdded((items) => [...items, candidate.name]); await Promise.all([mutate(), refresh()]); } setAdding(null); }
  if (!data) return <p className="p-6 text-slate-400">Analisi statistica in corso…</p>;
  const market = data.body; const candidates: Candidate[] = recommendations?.body.candidates || [];
  return <div className="space-y-5"><header className="rounded-3xl border border-amber-400/30 bg-gradient-to-br from-[#35200a] via-[#1E293B] to-[#162033] p-6"><Target className="mb-2 text-amber-400"/><p className="text-xs font-black uppercase tracking-widest text-amber-300">Mercato AI · dati reali</p><h2 className="mt-1 text-2xl font-black text-white">Rinforzi calcolati, non casuali</h2><p className="mt-3 text-xs leading-relaxed text-slate-300">{market.methodology}</p></header><div className="grid grid-cols-2 gap-3 md:grid-cols-4">{market.gaps.map((gap: any) => <button key={gap.role} onClick={() => setRole(gap.role)} className={`rounded-2xl border p-4 text-left ${role === gap.role ? 'border-amber-400 bg-amber-400/10' : 'border-slate-700 bg-slate-800'}`}><b className="text-lg text-white">{gap.role}</b><p className="mt-1 text-2xl font-black text-white">{gap.current}/{gap.target}</p><span className="text-xs font-bold text-amber-300">{gap.missing ? `Mancano ${gap.missing}` : 'Reparto coperto'}</span></button>)}</div>{role && <section className="rounded-3xl border border-amber-400/25 bg-slate-800 p-5"><h3 className="text-xl font-black text-white">Profili {role} ordinati per score</h3><p className="mt-1 text-xs text-slate-400">Sono mostrati solo giocatori con dati Serie A verificati.</p><div className="mt-4 space-y-3">{candidates.map((candidate) => <article key={candidate.name} className="rounded-2xl border border-slate-700 bg-slate-900 p-4"><div className="flex justify-between gap-3"><div><h4 className="font-black text-white">#{candidate.priority} · {candidate.name}</h4><p className="text-xs text-slate-400">{candidate.team} · valore {candidate.marketValue} · costo {candidate.costBand}</p></div><button disabled={adding === candidate.name || added.includes(candidate.name)} onClick={() => add(candidate)} className="flex h-9 items-center gap-1 rounded-xl bg-emerald-400 px-3 text-xs font-black text-slate-950 disabled:opacity-50">{added.includes(candidate.name) ? <><Check size={14}/>IN ROSA</> : <><Plus size={14}/>{adding === candidate.name ? '…' : 'AGGIUNGI'}</>}</button></div><div className="mt-3 grid grid-cols-3 gap-2 text-center text-xs"><div className="rounded-lg bg-slate-800 p-2"><b className="block text-lg text-amber-300">{candidate.score}</b>Score</div><div className="rounded-lg bg-slate-800 p-2"><b className="block text-lg text-white">{candidate.stats.rating}</b>rating medio</div><div className="rounded-lg bg-slate-800 p-2"><b className="block text-lg text-white">{candidate.stats.goalActions90}</b>G+A /90</div><div className="rounded-lg bg-slate-800 p-2"><b className="block text-lg text-white">{candidate.stats.appearances}</b>presenze</div><div className="rounded-lg bg-slate-800 p-2"><b className="block text-lg text-white">{candidate.stats.goals}</b>gol</div><div className="rounded-lg bg-slate-800 p-2"><b className={`block text-lg ${candidate.stats.formDelta >= 0 ? 'text-emerald-300' : 'text-red-300'}`}>{candidate.stats.formDelta >= 0 ? '+' : ''}{candidate.stats.formDelta}</b>forma</div></div><p className="mt-3 text-xs text-slate-400">{candidate.reason}</p></article>)}</div></section>}</div>;
}
